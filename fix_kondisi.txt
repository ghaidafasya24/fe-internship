Perubahan yang diperlukan:

1. Di Js/Temp/tabel_koleksi.js setelah baris "const tempat = item.tempat_penyimpanan || item.tempat || '-';" tambahkan:
   const kondisi = item.kondisi || '-';

2. Di tr.innerHTML sebelum deskripsi tambahkan:
   <td class="px-4 py-2 border-b">${kondisi}</td>

3. Di Template/admin/koleksi.html sebelum </form> tambahkan input:
   <input id="kondisi" name="kondisi" placeholder="Kondisi (Baik/Rusak/Perlu Perbaikan)" class="border rounded px-2 py-1 w-full" required />

4. Di Js/controller/add-collection.js di fungsi editKoleksi tambahkan:
   document.getElementById('kondisi').value = itemData.kondisi || '';
